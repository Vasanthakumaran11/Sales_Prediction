import React, { useState } from 'react';
import { 
  Settings as SettingsIcon, 
  Sun, 
  Moon, 
  Globe, 
  HelpCircle, 
  Shield, 
  ChevronDown, 
  ChevronUp, 
  CheckCircle,
  HelpCircle as QuestionIcon
} from 'lucide-react';

export default function Settings({ theme, setTheme }) {
  const [language, setLanguage] = useState('English');
  const [expandedFAQ, setExpandedFAQ] = useState(null);
  
  const faqs = [
    {
      q: "How do I initialize a new store profile?",
      a: "Go to the Workspace Initializer (Exit Workspace) and select 'New Store Setup'. Input the store type, location type, planned opening month, and initial investment. The platform automatically trains a base Hybrid Ensemble model and outputs a Pre-Launch Insights Deck."
    },
    {
      q: "What is the difference between Raw ML Signal and Adjusted Output?",
      a: "The Raw ML Signal shows the initial statistical forecast generated by our ensemble models. The Adjusted Output layers this prediction with 'Decision Intelligence' soft caps (e.g., storage limits of 400 or 800 items/day) and 'Market Realism' scaling (such as rural/semi-urban coefficients or a 40% Month 1 cold-start discount)."
    },
    {
      q: "How does the Consolidated Bulk Replenishment Tool calculate quantities?",
      a: "For multi-store environments, the Replenishment Tool aggregates individual ROP (Reorder Point) deficits across all active stores (Balaji, Shiva, Surya) and bundles them into a single consolidated supplier invoice to unlock volume discount scaling (15% base discount contract)."
    },
    {
      q: "How often should I retrain the personalized model?",
      a: "We recommend triggering a retraining cycle after logging 14 days of real sales transactions. This merges the synthetic base templates with localized consumer trends to increase R² score accuracy up to 94%."
    }
  ];

  const languages = ['English', 'Tamil', 'Hindi', 'Spanish', 'French', 'German'];

  const toggleFAQ = (index) => {
    setExpandedFAQ(expandedFAQ === index ? null : index);
  };

  return (
    <div className="space-y-6 font-sans">
      {/* Top Header Row */}
      <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 border-b border-zinc-200 dark:border-zinc-800 pb-4">
        <div>
          <h2 className="text-xl font-bold text-zinc-900 dark:text-white tracking-tight flex items-center gap-2">
            <SettingsIcon className="w-5 h-5 text-emerald-500" /> Platform Settings
          </h2>
          <p className="text-xs text-zinc-500 dark:text-zinc-400 mt-0.5">
            Configure display options, review terms of service, and access platform support documentation.
          </p>
        </div>
      </div>

      {/* Main Grid */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        
        {/* Left Columns: Config Options */}
        <div className="lg:col-span-2 space-y-6">
          
          {/* Section: Appearance (Theme) & Language */}
          <div className="bg-white dark:bg-zinc-900/40 border border-zinc-200 dark:border-zinc-800 rounded-2xl p-5 space-y-5">
            <h3 className="text-sm font-bold text-zinc-900 dark:text-white uppercase tracking-wider border-b border-zinc-150 dark:border-zinc-850 pb-2">
              Appearance & Localization
            </h3>
            
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
              {/* Theme Settings */}
              <div className="space-y-2">
                <label className="text-xs font-semibold text-zinc-550 dark:text-zinc-400 uppercase tracking-wider block">
                  Select Color Theme
                </label>
                <div className="grid grid-cols-2 gap-3">
                  <button
                    id="btn-theme-light"
                    onClick={() => setTheme('light')}
                    className={`flex items-center justify-center gap-2 py-3 rounded-lg text-xs font-semibold border transition-all ${
                      theme === 'light'
                        ? 'bg-zinc-100 dark:bg-zinc-800 border-zinc-300 dark:border-zinc-700 text-zinc-900 dark:text-white shadow-sm'
                        : 'bg-zinc-50 dark:bg-zinc-950/40 border-zinc-200 dark:border-zinc-900 text-zinc-550 dark:text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-300'
                    }`}
                  >
                    <Sun className="w-4 h-4 text-amber-500 animate-spin-slow" />
                    <span>Light Mode</span>
                  </button>
                  <button
                    id="btn-theme-dark"
                    onClick={() => setTheme('dark')}
                    className={`flex items-center justify-center gap-2 py-3 rounded-lg text-xs font-semibold border transition-all ${
                      theme === 'dark'
                        ? 'bg-zinc-105 dark:bg-zinc-800 border-zinc-300 dark:border-zinc-700 text-zinc-900 dark:text-white shadow-sm'
                        : 'bg-zinc-50 dark:bg-zinc-950/40 border-zinc-200 dark:border-zinc-900 text-zinc-550 dark:text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-300'
                    }`}
                  >
                    <Moon className="w-4 h-4 text-indigo-400" />
                    <span>Dark Mode</span>
                  </button>
                </div>
              </div>

              {/* Language Settings */}
              <div className="space-y-2">
                <label className="text-xs font-semibold text-zinc-550 dark:text-zinc-400 uppercase tracking-wider block">
                  Display Language
                </label>
                <div className="relative">
                  <select
                    id="select-language"
                    value={language}
                    onChange={(e) => setLanguage(e.target.value)}
                    className="w-full bg-zinc-50 dark:bg-zinc-950 border border-zinc-200 dark:border-zinc-850 rounded-lg px-4 py-2.5 text-zinc-900 dark:text-white focus:outline-none focus:border-emerald-500 text-xs appearance-none"
                  >
                    {languages.map(lang => (
                      <option key={lang} value={lang}>{lang}</option>
                    ))}
                  </select>
                  <Globe className="absolute right-3.5 top-3 w-4 h-4 text-zinc-400 pointer-events-none" />
                </div>
              </div>
            </div>
          </div>

          {/* Section: Help FAQ Accordion */}
          <div className="bg-white dark:bg-zinc-900/40 border border-zinc-200 dark:border-zinc-800 rounded-2xl p-5 space-y-4">
            <h3 className="text-sm font-bold text-zinc-900 dark:text-white uppercase tracking-wider border-b border-zinc-150 dark:border-zinc-850 pb-2">
              Help & Frequently Asked Questions
            </h3>
            
            <div className="space-y-2.5">
              {faqs.map((faq, idx) => {
                const isExpanded = expandedFAQ === idx;
                return (
                  <div 
                    key={idx} 
                    className="border border-zinc-150 dark:border-zinc-850 rounded-xl overflow-hidden transition-all bg-zinc-50/50 dark:bg-zinc-950/20"
                  >
                    <button
                      type="button"
                      onClick={() => toggleFAQ(idx)}
                      className="w-full px-4 py-3.5 text-left text-xs font-bold text-zinc-900 dark:text-zinc-200 hover:bg-zinc-100/50 dark:hover:bg-zinc-900/40 flex justify-between items-center transition-all"
                    >
                      <span className="flex items-center gap-2">
                        <QuestionIcon className="w-4 h-4 text-emerald-500 shrink-0" />
                        {faq.q}
                      </span>
                      {isExpanded ? <ChevronUp className="w-3.5 h-3.5 text-zinc-500" /> : <ChevronDown className="w-3.5 h-3.5 text-zinc-500" />}
                    </button>
                    {isExpanded && (
                      <div className="px-4 pb-4 pt-1 text-xs text-zinc-650 dark:text-zinc-400 leading-relaxed border-t border-zinc-150 dark:border-zinc-850/50">
                        {faq.a}
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          </div>

        </div>

        {/* Right Column: Terms of Service */}
        <div className="lg:col-span-1 bg-white dark:bg-zinc-900/40 border border-zinc-200 dark:border-zinc-800 rounded-2xl p-5 flex flex-col h-[500px] justify-between">
          <div className="space-y-1 border-b border-zinc-150 dark:border-zinc-850 pb-3">
            <h3 className="text-sm font-bold text-zinc-900 dark:text-white uppercase tracking-wider flex items-center gap-2">
              <Shield className="w-4 h-4 text-emerald-500" /> Terms of Service
            </h3>
            <p className="text-[10px] text-zinc-500">Legal outline regulating access and data governance constraints.</p>
          </div>

          <div className="flex-1 overflow-y-auto py-4 text-[10px] text-zinc-600 dark:text-zinc-400 space-y-3 leading-relaxed pr-1">
            <p className="font-bold text-zinc-800 dark:text-zinc-200 text-[11px]">1. Relational Cache Licensing</p>
            <p>
              By utilizing the Smart Retail AI platform, you agree to caching local daily transactions into a relational PostgreSQL database cache. This data is utilized solely for continuous k-fold model retraining logic.
            </p>
            <p className="font-bold text-zinc-800 dark:text-zinc-200 text-[11px]">2. Predictor Outputs Disclaimer</p>
            <p>
              Model suggestions (Safety stock levels, ROP limits, and EOQ ratios) are statistical probabilities calculated by a stacked Hybrid Ensemble ML engine. These are operational suggestions only and do not constitute contract guarantees for store asset volumes.
            </p>
            <p className="font-bold text-zinc-800 dark:text-zinc-200 text-[11px]">3. Capital Allocation Constraints</p>
            <p>
              The Decision Intelligence layer adjusts forecast signals to match physical store formats. The system holds no liability for supply bottlenecks arising from manual capacity overrides of these safety metrics.
            </p>
            <p className="font-bold text-zinc-800 dark:text-zinc-200 text-[11px]">4. Multi-Tenant Sync</p>
            <p>
              Executive control towers sync metrics across Balaji, Shiva, and Surya branch profiles. Consolidated purchase aggregations represent vendor contracts generated at runtime.
            </p>
          </div>

          <div className="p-3 rounded-lg bg-zinc-50 dark:bg-zinc-950 border border-zinc-200 dark:border-zinc-850 text-[10px] text-zinc-500 flex items-center gap-2 mt-2">
            <CheckCircle className="w-4 h-4 text-emerald-500 shrink-0" />
            <span>Policy Status: <strong>Version 1.0.4 Active</strong></span>
          </div>
        </div>

      </div>
    </div>
  );
}
